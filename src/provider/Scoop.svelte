<script lang="ts">
    // import Provider, {ScoopProvider} from "../lib/ProviderBody.svelte";
    import Provider from "../lib/Provider.svelte";

    import {scoopProvider} from "../lib/providers";
</script>

<Provider id={scoopProvider.id} />
